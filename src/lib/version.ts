// Single source of truth for the app version shown in Settings → Software
// update. Bump APP_VERSION (and the package.json version) on each release so
// members can see whether they are on the latest build after refreshing.
export const APP_VERSION = "1.0.1";

// ISO date of this build/release, shown next to the version.
export const APP_BUILD_DATE = "2026-07-12";
